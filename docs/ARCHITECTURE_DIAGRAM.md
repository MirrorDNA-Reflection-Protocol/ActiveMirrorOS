# ActiveMirrorOS Architecture Diagram

## System Layers

```
┌─────────────────────────────────────────────────────────┐
│                    USER APPLICATIONS                    │
│  (Your CLI, Web App, Mobile App, Desktop App, etc.)    │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ Uses SDK
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  ACTIVEMIRROR SDK                       │
│  ┌──────────────────────────────────────────────────┐  │
│  │          ActiveMirror (Orchestrator)             │  │
│  └───────┬──────────────────────────────────────────┘  │
│          │                                              │
│  ┌───────▼──────┬────────────┬────────────┬──────────┐ │
│  │   Session    │ Reflective │   Vault    │ Storage  │ │
│  │   Manager    │   Client   │  Memory    │ Backends │ │
│  └──────────────┴────────────┴────────────┴──────────┘ │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ Implements/Uses
                     ▼
┌─────────────────────────────────────────────────────────┐
│              PROTOCOL & FOUNDATION LAYER                │
│  ┌──────────┬──────────┬──────────┬──────────────────┐ │
│  │MirrorDNA │  LingOS  │ Storage  │ Cryptography     │ │
│  │Protocol  │ Patterns │ Formats  │ (AES-256-GCM)    │ │
│  └──────────┴──────────┴──────────┴──────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## Component Architecture

```
ActiveMirrorOS/
│
├─── Python SDK ────────────────────┬─── JavaScript SDK ───────────┐
│                                   │                              │
│  ┌──────────────────────────┐    │  ┌──────────────────────┐   │
│  │  activemirror/           │    │  │  activemirror.js     │   │
│  │  ├── core/               │    │  │  memory.js           │   │
│  │  │   ├── mirror.py       │    │  │  reflective-client.js│   │
│  │  │   ├── session.py      │    │  │  vault.js            │   │
│  │  │   ├── config.py       │    │  └──────────────────────┘   │
│  │  │   └── message.py      │    │                              │
│  │  ├── storage/            │    │  Same capabilities,          │
│  │  │   ├── sqlite.py       │    │  different implementation    │
│  │  │   ├── memory.py       │    │                              │
│  │  │   └── base.py         │    │  JSON storage vs SQLite      │
│  │  ├── reflective_client.py│    │  Node.js crypto vs          │
│  │  ├── vault_memory.py     │    │  Python cryptography        │
│  │  └── integrations/       │    │                              │
│  └──────────────────────────┘    │                              │
│                                   │                              │
└───────────────────────────────────┴──────────────────────────────┘
```

## Memory Model

```
┌─────────────────────────────────────────────────────────┐
│                     APPLICATION                         │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │   SESSION MEMORY      │  ◄── In-Memory (RAM)
         │   (Active Context)    │      Fast, Temporary
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ PERSISTENT STORAGE    │  ◄── On-Disk
         │  (SQLite / JSON)      │      Durable, Queryable
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │   VAULT MEMORY        │  ◄── Encrypted Storage
         │ (AES-256-GCM)         │      Secure, Private
         └───────────────────────┘
```

## Data Flow

```
User Input
    │
    ▼
┌─────────────────┐
│ ActiveMirror    │
│ .send()         │
└────────┬────────┘
         │
         ├──► Session Manager ──► Add to Context
         │
         ├──► Reflective Client ──► Generate Response
         │                          (with patterns)
         │
         ├──► Storage Backend ──► Persist Message
         │    (SQLite/JSON)
         │
         └──► Vault Memory ────► Store Sensitive Data
              (if marked)         (encrypted)
```

## Session Lifecycle

```
1. CREATE SESSION
   mirror.create_session("session-id")
   │
   ▼
2. ACTIVE SESSION (RAM)
   session.add_message(role, content)
   session.get_context()
   │
   ▼
3. PERSIST (Disk)
   Auto-saved or manual save
   │
   ▼
4. RESUME LATER
   mirror.load_session("session-id")
   │
   ▼
5. CONTINUE or ARCHIVE
   Continue using or export/archive
```

## Storage Backend Options

```
┌─────────────────────────────────────────────┐
│          Storage Interface                  │
└─────────────┬───────────────────────────────┘
              │
      ┌───────┼───────┬────────────┐
      │       │       │            │
      ▼       ▼       ▼            ▼
   ┌─────┐ ┌────┐ ┌──────┐    ┌────────┐
   │SQLite│ │JSON│ │Memory│    │Custom  │
   │      │ │    │ │      │    │(extend)│
   └─────┘ └────┘ └──────┘    └────────┘

   Production  Portable  Testing   Your Choice
```

## Reflective Patterns

```
User Message
    │
    ▼
┌──────────────────────┐
│  Reflective Client   │
│                      │
│  Choose Pattern:     │
│  ┌────────────────┐  │
│  │ ◊ Exploratory  │──► Open-ended inquiry
│  │ ✦ Analytical   │──► Critical examination
│  │ ★ Creative     │──► Divergent thinking
│  │ ⬢ Strategic    │──► Goal-oriented planning
│  └────────────────┘  │
│                      │
│  Add Uncertainty:    │
│  ⟨low⟩ ⟨medium⟩ ⟨⟨high⟩⟩
│                      │
└──────────┬───────────┘
           │
           ▼
    Reflective Response
```

## Encryption Flow (Vault)

```
Sensitive Data
    │
    ▼
Password/Key
    │
    ├──► PBKDF2 (100k iterations)
    │    │
    │    ▼
    │   Derived Key
    │    │
    ▼    ▼
┌─────────────────┐
│  AES-256-GCM    │
│  Encryption     │
│  + Unique IV    │
└────────┬────────┘
         │
         ▼
  Encrypted File
  (on disk)
```

## Cross-SDK Compatibility

```
Python Session          JavaScript Session
      │                        │
      │   JSON Export          │
      ├────────────────────────┤
      │   Compatible Format    │
      ├────────────────────────┤
      │   JSON Import          │
      │                        │
      ▼                        ▼
   Can work with same data files
   (if using JSON storage)
```

## Integration Points

```
┌─────────────────────────────────────────────┐
│           ActiveMirrorOS                    │
└───────┬─────────────────────────────────────┘
        │
        │ Integrates with:
        │
        ├──► MirrorDNA Protocol (Identity/Continuity)
        │
        ├──► LingOS (Reflective Dialogue Patterns)
        │
        ├──► Custom LLM APIs (OpenAI, Anthropic, Local)
        │
        ├──► Storage Backends (SQLite, Postgres, etc.)
        │
        └──► Custom Extensions (Your Integration)
```

## Example Application Stack

```
┌─────────────────────────────────────┐
│   Your Web App (React/Vue/etc.)    │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│   Backend API (Express/Flask)      │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│   ActiveMirror SDK                 │
│   (Python or JavaScript)           │
└─────────────────┬───────────────────┘
                  │
                  ├──► SQLite (local dev)
                  └──► PostgreSQL (production)
```

## Summary

**ActiveMirrorOS provides:**
- 🧠 Memory orchestration layer
- 💾 Flexible storage backends
- 🔐 Built-in encryption
- 🎭 Reflective patterns
- 🌐 Cross-platform SDKs
- 🔌 Extensible architecture

For detailed component documentation, see [architecture.md](architecture.md).
